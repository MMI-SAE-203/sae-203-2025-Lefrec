---
const {titre, photo_URL} = Astro.props;
---

<div class="relative flex -mx-8 -mt-8 mb-8 h-[300px]">
    <h1 class="absolute self-center text-center w-full px-5 text-light font-heading text-4xl z-1">{titre}</h1>
    {photo_URL ? <img class="object-cover w-full grayscale-50" src={photo_URL} alt=""> : <div class="bg-dark w-full h-full"></div>}
</div>