---
const { titre, photo_URL, description, lien, unique } = Astro.props;
---

<div class="flex flex-col gap-2 border p-2 rounded-lg">
    <a class={unique? "w-full" : 'w-[300px]'} href={lien}>
        <img class="w-full h-[150px] object-cover rounded-md" src={photo_URL} alt="Photo d'illustration de la section" />
    </a>
    <p class="font-heading font-light text-lg">{titre}</p>
    <p class="line-clamp-3">{description}</p>
    <a class="btn self-end" href={lien}>Y aller</a>
</div>
