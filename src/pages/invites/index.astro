---
import Layout from "../..//layouts/Layout.astro";
import CardInvite from "../../components/CardInvite.astro";

import { allInvites } from "../../../backend/backend.mjs";
const invites = await allInvites();
console.log(invites);
---

<Layout titre="Les invités">
    <section class="section col-span-6">
        <p class="mb-4">
            Pour cette première édition, <strong>CinéFlash 25</strong> a l'honneur
            d'accueillir des invités exceptionnels issus de divers horizons du monde
            cinématographique. <strong
                >Réalisateurs, acteurs, scénaristes et personnalités locales</strong
            > seront présents pour enrichir le festival par leurs expériences, leurs
            talents et leur passion du cinéma.
        </p>
        <h3>Édition 2025</h3>
        <div
            class="grid grid-cols-[repeat(auto-fit,minmax(116px,1fr))] gap-4 my-8 lg:grid-cols-[repeat(auto-fit,minmax(216px,1fr))] lg:gap-8"
        >
            {invites.map((invite) => <CardInvite {...invite} />)}
        </div>
        <div class="w-full h-[2px] bg-ton"></div>
    </section>
</Layout>
